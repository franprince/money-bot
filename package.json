{
  "name": "money-bot",
  "version": "1.0.0",
  "description": "Telegram bot to track personal expenses",
  "main": "src/index.ts",
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "start": "bun run src/index.ts",
    "test": "bun test"
  },
  "dependencies": {
    "grammy": "^1.31.0"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
