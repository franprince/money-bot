After each change, run the tests to verify that the changes are correct.
After the tests pass, commit the changes using the conventional commits format and making the commits as atomic as possible.
Write the functional requirements of the changes you are about to make in the CHANGELOG.md file. This file should be updated after each change.
The bot should send the last changes in the CHANGELOG.md file to the user after each change.
The bot should send the pending reminders to the user after each reboot.